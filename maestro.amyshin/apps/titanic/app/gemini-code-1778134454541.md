# FastAPI Titanic (James) Context

이 파일은 Cursor AI가 프로젝트의 초기 상태를 학습하기 위한 문서입니다.

## 1. 초기화 코드 (`james.py`)

```python
from fastapi import FastAPI
from walter import Walter

app = FastAPI(title="Titanic (James)")

class James:
    def __init__(self):
        pass

@app.get("/")
def read_root():
    return {"message": "FAST API 초기화 성공", "docs": "/docs"}

if __name__ == "__main__":
    import uvicorn
    uvicorn.run("james:app", host="127.0.0.1", port=8000, reload=True)